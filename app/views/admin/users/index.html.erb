<div class="container">
	<table>
		<% @users.each do |user| %>
			<tr>
				<td>
					<%= link_to admin_user_path(user) do %>
						<%= attachment_image_tag user, :image, size:'50x50', format: 'jpeg', fallback: 'no-user-image.png' %>
					<% end %>
				</td>
				<td><%= user.name %></td>
				<td><%= 'how many posts' %></td>
				<td>followings/followers</td>
			</tr>
		<% end %>
</table>
</div>