<div class="container">
	<div class="row">
		<% @photos.each do |photo| %>
			<div class="col-md-3">
				<%= link_to photo_path(photo.id) do %>
					<%= attachment_image_tag photo, :image, size:'100x100', format: 'jpeg' %>
				<% end %>
			</div>
		<% end %>
	</div>
</div>