<div class="container">
	<div class="row">

		<%= attachment_image_tag @photo, :image, size:'300x300', format: 'jpeg' %>

		<%= @photo.text %>

		<% if @photo.user_id == current_user.id %>
			<%= link_to 'Edit', edit_photo_path %>
			<%= link_to 'Destroy', photo_path, method: :delete, data: {confirm: "Are you sure?"} %>
		<% end %>

	</div>
</div>